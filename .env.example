# Root-level env for docker-compose infra services (postgres/redis/minio).
# Backend runtime config: see backend/.env.example
# Frontend runtime config: see frontend/.env.local

DB_NAME=whatsapp_platform
DB_USER=postgres
DB_PASSWORD=postgres123
DB_PORT=5433

REDIS_PASSWORD=redis123
REDIS_PORT=6380

MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin123
